/**
 * Root application component.
 *
 * Rendered once by {@link main.tsx} inside a React `StrictMode` tree.
 * Acts as the top-level layout shell – all pages and global providers should
 * be composed here as the project grows.
 *
 * @returns The root JSX element of the application.
 *
 * @example
 * ```tsx
 * // Rendered automatically by main.tsx; no manual instantiation needed.
 * <App />
 * ```
 */
function App() {
  return <h1>Hello World</h1>
}

export default App
